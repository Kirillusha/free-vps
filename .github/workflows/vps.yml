name: Free VPS
on: [workflow_dispatch]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: |
          sudo apt update
          sudo apt install openssh-server -y
          sudo systemctl start ssh
          sudo echo "root:пароль123" | sudo chpasswd
          sudo service ssh restart
          curl -s https://serveo.net | sh
